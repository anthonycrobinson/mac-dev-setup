Always check shell scripts before running them. ESPECIALLY when sudo is requested as below...

```curl -sSL https://raw.githubusercontent.com/anthonycrobinson/mac-dev-setup/master/setup.sh | sudo bash -s```

